1. Extract the ZIP file to a folder
Install Expo CLI
Open Command Prompt/Terminal
Type: npm install -g @expo/cli

2. Install Project Dependencies
In VS Code, open the Terminal: View → Terminal (or press Ctrl+ / Cmd+)
Make sure you're in the project directory (you should see files like package.json)
Type: npm install
After installation 
3. Run App
3.1 Start the Development Server
In the VS Code terminal, type: npm run dev
Wait for the server to start
You'll see a QR code and some options
3.2 View App
Option A: Web Browser

Press w in the terminal to open in web browser
 app will open at http://localhost:8081
